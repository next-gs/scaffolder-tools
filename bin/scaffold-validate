#!/usr/bin/env ruby
$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))

require 'rubygems'
require 'configliere'
require 'scaffold_validate'

Settings.use :commandline
Settings.resolve!

ScaffoldValidate.new(Settings).run
