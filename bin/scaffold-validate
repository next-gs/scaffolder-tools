#!/usr/bin/env ruby
$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))

require 'rubygems'
require 'scaffolder'
require 'scaffold_validate'

print ScaffoldValidate.run(ARGV[0],ARGV[1])
exit 0
