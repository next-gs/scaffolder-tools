#!/usr/bin/env ruby
$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))

require 'rubygems'
require 'configliere'
require 'scaffold_to_sequence'

Settings.use :commandline
Settings({:no=>{:sequence=>{:hash=>false}}})
Settings.resolve!

Scaffold2sequence.new(ARGV,Settings).run
